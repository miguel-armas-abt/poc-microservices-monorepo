spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        show_sql: true
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:db_menu_options;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;EXISTS=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password: