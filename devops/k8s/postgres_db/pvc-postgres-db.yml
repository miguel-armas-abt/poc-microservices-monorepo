kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-postgres-db
  labels:
    app: postgres-db
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi