{{- if .Values.secrets }}
  {{- range $name, $secret := .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $name }}
type: Opaque
data:
  {{- range $key, $value := $secret }}
    {{ $key }}: "{{ $value | b64enc }}"
    {{- end }}
---
{{- end }}
  {{- end }}